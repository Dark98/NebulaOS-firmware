# Boot Warning Audit

Structured inventory of every warning/error in the custom Nebula Pad Linux 6.6.18-rt23 kernel boot
log, per the "Nebula Pad Linux 6.6 boot-cleanup investigation and remediation" mission (2026-07-23).
Baseline: `artifacts/parity/baseline-2026-07-23-boot-cleanup/` (git commits, config/artifact hashes,
full `dmesg` capture). Evidence for every row below is a direct source-code citation or a live
stock/custom comparison - no message is classified from guesswork.

Log scope note: the captured log runs from cold boot through `Run /linuxrc as init process` only
(dmesg's own natural end point, before userspace). Userspace-stage validation (Wi-Fi association,
DHCP, Moonraker, Klipper, GuppyScreen, `confirm-good`, clean shutdown) is a separate, already-proven
capability documented in FIRMWARE.md and re-confirmed live at baseline time (see `BASELINE.md`).

Post-fix capture: `artifacts/parity/baseline-2026-07-23-boot-cleanup/dmesg-after-phase2-5-6-12.txt` -
full `dmesg` from the rebuilt/reflashed system carrying commits `0e3b5ab02`/`9d32ff87e` (kernel fork)
and `1cce6e0`/`24e0e6c`/`5b8018e` (main repo), captured after a real reboot cycle and re-confirmed
Moonraker-healthy.

| Message | Timestamp | Source file/function | Stock behavior | Runtime impact | Classification | Fix | Commit |
|---|---:|---|---|---|---|---|---|
| `ingenic-dma 13420000.dma: error -ENXIO: IRQ pdmam not found` | 0.000000 | `module_drivers/drivers/dma/ingenic/ingenic_dma.c:1173-1176` - `platform_get_irq_byname(pdev, "pdmam")`, only logged if `< 0`, DMA driver continues (`INGENIC SoC DMA initialized` follows immediately) | Not yet compared (low priority - non-fatal, SoC-level, base dtsi doesn't name a `pdmam` IRQ for this DMA controller instance) | None observed - DMA works (used throughout boot for eMMC/WiFi/video) | `OPTIONAL_FEATURE_MISSING` | None planned - `pdmam` is an optional secondary DMA-MCU interrupt the base SoC DT doesn't wire; primary DMA channels work throughout boot without it | - |
| `gpiod_set_value_cansleep: invalid GPIO (errorpointer)` (×2) | 1.326183, 1.361287 | `drivers/gpio/gpiolib.c:2153` (`validate_desc()`, mainline gpiolib safety net) called from `module_drivers/drivers/net/ethernet/ingenic/ingenic_mac.c:113,119` (`ingenic_mac_phy_hwrst()`) | n/a - stock is Linux 4.4.94, different driver generation | None observed this boot (Ethernet unused) but is a real caller-side bug: `gpiod_set_value_cansleep(lp->reset_gpio, ...)` called on a descriptor never checked with `IS_ERR()` | `REAL_KERNEL_BUG` | **FIXED, verified live** - root cause was a genuine pin conflict (`GPB-16` claimed by both `lcd_rst` and `mac1`'s `ingenic,rst-gpio`) that the driver's `== NULL` check (instead of `IS_ERR_OR_NULL()`) silently swallowed. Rebuilt/reflashed/rebooted: `dmesg \| grep -c 'invalid GPIO'` now returns `0`. | `0e3b5ab02` (kernel fork) |
| `ingenic_gpio_request: GP:GPD used_pins_bitmap: 0X07FE3F3C` + full backtrace + `module_drivers/drivers/pinctrl/pinctrl-ingenic.c:gpio functions has redefinition` | 1.069000-1.242822 | `module_drivers/drivers/pinctrl/pinctrl-ingenic.c:674` (bitmap check inside `ingenic_gpio_request()`, the vendor `.request` gpio_chip callback) **and** `pinctrl-ingenic.c:1277` (`jzgc->used_pins_bitmap \|= grp->pinmux_bitmap;`, a second, independent bitmap-OR that fires whenever ANY pinctrl group covering this bank is parsed/applied - not gated behind any conflict check at all) | Not compared (different kernel generation) | None observed - eMMC and WiFi both fully functional; the underlying `pinctrl_gpio_request()` call happens regardless of this warning and succeeds | `RESOLVED` (was `REAL_TWO_DEVICE_PIN_CONFLICT`) | **Resolved as a side effect of the BAIC4 audio investigation, verified live.** Root cause found via temporary instrumentation (kernel fork `41598d0b3`, reverted `8d4756c62`): the conflicting pin was `GPD-4` (global gpio 100), also covered by `baic4_pd` (`ingenic,pinmux = <&gpd 2 5>`, `x2000-pinctrl.dtsi:576`), which `&as_be_baic` selected as its own `pinctrl-0` (`halley5_v30.dts:721-724`). The separate, later BAIC4/DMIC investigation (`docs/BAIC4_AUDIO_INVESTIGATION.md`) removed that `pinctrl-0` claim entirely (`cafd5986c`) once BAIC4's DAI links were confirmed unreachable dead references - not done to fix this warning, but it did. Final cleanup mission rebuild/reflash/reboot (2026-07-23): full post-boot `dmesg` (234 lines) has zero matches for `redefinition`, `used_pins_bitmap`, `GPD`, `conflict`, `already requested`, `Call Trace`, or `backtrace`. See `docs/PIN_OWNERSHIP_MAP.md`. | `41598d0b3` (instrumentation, reverted `8d4756c62`), `cafd5986c` (actual resolution) |
| `Alternate GPT is invalid, using primary GPT.` | 1.368659 | Generic kernel EFI/GPT partition code (`block/partitions/efi.c`) | **Confirmed present verbatim in stock's own dmesg** (`artifacts/parity/stock/11-dmesg.txt:51`) | None - primary GPT is valid and used, all 10 partitions enumerate correctly on both systems | `STOCK_STORAGE_LAYOUT_LIMITATION` | None - per the mission's own decision rule ("stock shows the same warning → classify, document, do not modify the live disk"), this is closed. No GPT repair planned or needed. | - |
| `ingenic-sfc 13440000.sfc: unknown flash type` / `probe of 13440000.sfc failed with error -22` | 1.313320, 1.318705 | `module_drivers/drivers/mtd/devices/ingenic_sfc_v2/ingenic_sfc_drv.c:118,152` | `/sys/bus/platform/devices/13440000.sfc` is present on stock, but **`/proc/mtd` and `/sys/class/mtd` are both empty (zero devices) on stock too** - confirmed live, read-only, this pass | None - clean probe failure, doesn't block eMMC/anything else | `UNPOPULATED_REFERENCE_DESIGN_BLOCK` | **FIXED, verified live.** Converging evidence closed the earlier ambiguity: `/proc/mtd`/`/sys/class/mtd` empty on both stock and custom, no SPI-NOR/NAND consumer anywhere, and the node's own `uboot`/`kernel`/`rootfs` partition scheme doesn't match this board's real eMMC-based OTA A/B architecture. `&sfc` disabled in the board DTS. Rebuilt/reflashed/rebooted: no SFC probe attempt at all, eMMC/WiFi/display unaffected. | `212dce2f2` |
| `ingenic-mscaler 13702300.mscaler: error -ENXIO: IRQ index 0 not found` / `No MScaler IRQ specified` (×2, both instances) | 0.880896-0.902763 | `module_drivers/drivers/media/platform/ingenic-isp/mscaler.c:1443` (`dev_warn`, not `dev_err` - probe continues) | Base `x2000.dtsi:859-923` defines both `mscaler0`/`mscaler1` with `status = "ok"` but **no `interrupts` property at all** - this is an SoC-reference-level DT omission, not board-specific | None observed - rotation (`/dev/video0`), H.264 encode (`/dev/video1`), H.264 decode (`/dev/video2`) all register successfully; driver's own `dev_warn` (not `dev_err`) confirms this is treated as a supported, non-fatal configuration (interrupt-less/polling) | `UNUSED_REFERENCE_DESIGN_BLOCK` | **FIXED, pending live verification.** Final cleanup mission Phase 4: MScaler registers only as a `v4l2_subdev` ISP-pipeline component, not a `/dev/videoN` capture device - zero userspace consumer anywhere on this rootfs references "mscaler" or issues `VIDIOC` ioctls against it (grepped live), no `mscaler`-specific node under `/sys/class/video4linux`, and the real, working camera path (USB UVC via `ustreamer`) and rotation/H.264 encode/decode (separate SoC blocks) are entirely unaffected. Both instances disabled in the board DTS. | `69d8655d0` (kernel fork) |
| `x2000-sound sound: ASoC: CPU DAI DMIC not registered` / `snd_soc_register_card failed -517` (repeated ~6×) | 0.753917-1.617289 | `sound/soc/soc-core.c:1083` (mainline `dev_info`, not even a warning) + `module_drivers/sound/soc/ingenic/boards/halley5_v20.c` DMIC DAI link definition (lines 414-415, 527-531) | Already documented: `docs/STOCK_PARITY_MATRIX.md` - stock has a real, populated `134da000.as-dmic` device node; custom does not | None fatal - repeated `-EPROBE_DEFER` (517) retries are the standard, harmless ASoC deferred-probe pattern; no soundcard registers on custom (`ALSA device list: No soundcards found`), matching the already-known, already-scoped audio gap | `INVALID_REFERENCE_DESIGN_LINK`, superseded by full disable | **FIXED, verified live** (beeper/BAIC4 investigation mission), then **superseded**: `&as_dmic` had already been correctly disabled for a real `GPC-21`/`uart1` conflict, making DMIC a dangling reference with zero possible producer. Removed the unreachable `DMIC`/`BAIC4` DAI links (`cafd5986c`) and the six now-dangling `audio_map[]` routes (`cc86e471d`) - genuinely fixed, verified live (`/proc/asound/cards` went from empty to a real registered card at the time). The functional production-baseline mission's Phase 1/2 then found stock never uses ALSA at all and disabled the whole graph (`0ad06fa0e`, see below) - this specific fix's relevance is now superseded, not wrong; `halley5_v20.c`'s DAI link array itself is unreachable dead code again, this time by design. | `cafd5986c`, `cc86e471d`, `0ad06fa0e` (kernel fork) |
| `as-dsp 134d4000.as-dsp: ASoC: mux LO0_MUX has incorrect number of controls` (repeated for `LO0_MUX` through `LO11_MUX`, 12×) | 0.741-0.881 | `module_drivers/sound/soc/ingenic/as-v2/as-dsp.c` - `dapm_widgets[i].kcontrol_news`/`.num_kcontrols` assignment was wrapped in `#if 0`/`#endif` (dead vendor code), leaving `snd_soc_dapm_mux`-type widgets with `num_kcontrols == 0`; mainline `dapm_new_mux()` requires exactly `1` | n/a - SoC/driver-level, not board-specific | None fatal - `as-dsp` probes successfully regardless; purely a mainline strict-validation mismatch against already-correct-but-disabled vendor code (`lomux_enum[]`/`lomux_controls[]`, `LOMUX_SOC_DAPM_ENUM_EXT(0)`-`(15)`, already correctly sized for `LO_PORT_MAX_NUM=16`) | `REAL_KERNEL_BUG` (dead code, not a design gap), superseded by full disable | **FIXED, verified live**, then **superseded**: the dead-code fix (`4579e3803`) genuinely worked (confirmed live - the "incorrect number of controls" errors were replaced by a normal-severity "has no paths" message, proving the mux widgets registered correctly). The functional production-baseline mission's Phase 1/2 then found stock never uses ALSA at all and disabled the entire PCM/ALSA graph including `as-dsp` itself (`0ad06fa0e`) - `as-dsp` no longer probes, so these widgets no longer exist at all. Both fixes are real and correctly layered; the second simply supersedes the first's relevance. | `4579e3803`, `0ad06fa0e` (kernel fork) |
| `parse cpu-ost-iomap, ost number define in dt is too large!` / `parse cpu-intc-iomap, intc define in dt is too large!` | 0.000000 (×2, one per subsystem) | `module_drivers/drivers/clocksource/ingenic_core_ost.c` and `module_drivers/drivers/irqchip/irq-ingenic-chip.c` - identical off-by-one bug in both: a post-increment value-count check (`index > NR_CPUS * 2 - 1`) fired on the *last legitimate* `cpu-ost-map`/`cpu-intc-map` entry itself, for this board's fully correct, exactly-`NR_CPUS`-pair (2 pairs, `CONFIG_NR_CPUS=2`) DT - never an actual excess | n/a - SoC/driver-level | None - both CPUs always started fine, clocksource/interrupt controller always initialized correctly; every real DT value was already stored correctly before the old check ever fired | `REAL_KERNEL_BUG` (off-by-one, not a DT authoring error) | **FIXED, verified live.** Functional production-baseline mission Phase 3: confirmed via full source trace that removing/reducing the DT's `cpu-ost-map`/`cpu-intc-map` entries would have been wrong (they're already minimal and correct for 2 CPUs) - fixed both drivers instead, checking the target pair index against the real `cpu_ost_map[NR_CPUS]`/`cpu_intc_map[NR_CPUS]` array bound *before* writing, rather than after the fact against a flat value count. Strictly safer than the original for a genuinely-oversized DT too (old code could perform one out-of-bounds write before its check caught up). Rebuilt/reflashed/rebooted: zero "too large" matches anywhere in a fresh boot log. | `2d507671c`, `e123bb14f` (kernel fork) |
| `x2000-sound sound: Sound Card successed` / `Speaker enable pin(34) request ok` / all `as-platform`/`as-baic`/`as-mixer`/`as-spdif`/`icodec` probe-success lines | 0.7-0.9 | Entire PCM/ALSA graph - see the DMIC row above and `docs/PIN_OWNERSHIP_MAP.md`'s GPIO 34 section | Stock: `ALSA device list: No soundcards found.` - confirmed live by booting the exact stock firmware, zero `/proc/asound` entries beyond the generic timer, and stock's real production stack (Klipper/GuppyScreen) has zero consumer for the on-disk-but-inactive `audio_setting.sh`/`audio-server` legacy binary family | None - not a warning at all, these were all successful, harmless probe messages; removing them is a capability-scope decision (stock parity), not a bug fix | `STOCK_EQUIVALENT_ACCEPTED` (whole graph now matches stock: no card) | **Whole graph disabled, verified live.** Functional production-baseline mission Phase 1/2: `as-platform`, `as-virtual-fe`, `as-fmtcov`, `as-dsp`, `as-baic`, `as-dmic`, `as-mixer`, `as-spdif`, `icodec`, and the top-level `sound` machine-driver node all disabled (`0ad06fa0e`). Rebuilt/reflashed/rebooted: `dmesg` now reads `ALSA device list: No soundcards found.` - byte-for-byte matching stock's own message. PWM beeper unaffected (`guppybeep click` → RC=0, confirmed live post-reboot). | `0ad06fa0e` (kernel fork) |
| `eth%d: no PHY found` / `dwc-mac 134a0000.mac: MII Probe failed!` | 1.402433, 1.405583 | `module_drivers/drivers/net/ethernet/ingenic/ingenic_mac.c`, board override `halley5_v30.dts:575-586` (`&mac1`, `status = "okay"`) | Not yet compared (stock is a different kernel generation) | None - no product feature on this board uses wired Ethernet (Nebula Pad/Ender3V3KE has no RJ45 port) | `INTENTIONAL_UNUSED_BLOCK` (high confidence) | **FIXED, verified live** - root cause: `ingenic,rst-gpio` (`&gpb 16`) collided with `lcd_rst`'s live GPIO ownership of the same pin (confirmed via live debugfs: `gpio-48 (lcd_rst) out hi`). `&mac1` disabled in the board DTS. Rebuilt/reflashed/rebooted: `dmesg \| grep -iE 'mac1\|134a0000'` now returns zero matches - no probe attempt at all. | `9d32ff87e` (kernel fork) |
| `platform regulatory.0: Direct firmware load for regulatory.db failed with error -2` / `cfg80211: failed to load regulatory.db` | 1.612460, 1.621406 | Mainline `cfg80211` regulatory core | n/a - stock uses a different wireless stack generation, not a useful comparison | None - `iw reg get` now returns a real, populated regulatory database (channel bands, DFS info) instead of a bare permissive fallback | `OPTIONAL_FEATURE_MISSING` (now fully fixed) | **FIXED, verified live.** `scripts/firmware/fetch-wireless-regdb.sh` (Phase 5b) stages a hash-verified copy of the exact upstream release Buildroot's own package pins directly under the overlay, closing the `CONFIG_EXTRA_FIRMWARE` gap the previous pass left open. Rebuilt/reflashed/rebooted: **no failure message at all** (only the harmless "Loading compiled-in X.509 certificates" line remains), and `iw reg get` confirms a real, non-fallback regulatory database is active. | `1cce6e0`, `e89226a` |
| `brcmfmac mmc1:0001:1: Direct firmware load for brcm/brcmfmac43430-sdio.ingenic,halley5.bin failed with error -2` | 2.320625 | Mainline `brcmfmac` board-specific firmware alias lookup; `scripts/build/overlay/lib/firmware/brcm/` **only shipped the generic `brcmfmac43430-sdio.bin`/`.txt`**, not the board-specific alias name | n/a | None - generic firmware loads successfully immediately after (`Firmware: BCM43430/1 ... version 7.46.58.13`), WiFi fully functional | `OPTIONAL_FEATURE_MISSING` (real, easily fixable cosmetic gap) | **FIXED, verified live** - two-part fix: a symlink alias in the overlay (`24e0e6c`) was necessary but not sufficient, since `brcmfmac`'s firmware request happens before rootfs is mounted (same already-documented timing class as the primary binary); baking the alias into `CONFIG_EXTRA_FIRMWARE` (`5b8018e`) closed it. Rebuilt/reflashed/rebooted: the "failed" message for this alias no longer appears in `dmesg` at all. | `24e0e6c`, `5b8018e` |
| `brcmfmac mmc1:0001:1: Direct firmware load for brcm/brcmfmac43430-sdio.clm_blob failed with error -2` / `no clm_blob available` / `no txcap_blob available` | 2.340321-2.459528 | Mainline `brcmfmac` optional CLM/TxCap blob loader | **Confirmed absent on stock itself**, not merely "not found here" - see provenance note below | Low - `brcmf_c_process_clm_blob`'s own message says "device may have limited channels available", not "non-functional"; WiFi already proven working end-to-end | `STOCK_EQUIVALENT_ACCEPTED` | **Closed, no blob to add.** Final cleanup mission Phase 7: extracted stock's real rootfs from a full raw device backup (`vendor/device-backups/nebula_system_backup.bin`, GPT partition 7, `sgdisk`+`dd`+`unsquashfs`, no root required) and confirmed via SHA256 that our currently-deployed `brcmfmac43430-sdio.bin`/`.txt` are **byte-identical** to stock's own `cyw43438-7.46.58.13.bin`/`nvram_azw372.txt`. Stock's own init scripts (`S43wifi_bcm_init_config`, `S44wifi_bcm_up`) contain zero "clm"/"txcap" references, and no such file exists anywhere in stock's shipped rootfs. This is a definitive "confirmed absent upstream," not an unresolved search - per the mission's own provenance-priority framework, a public/generic-chip-family candidate was correctly never installed, since none is proven to originate from this exact device. | - |
| `mmc1: Failed to initialize a non-removable card` | 1.672314 | Generic MMC core `mmc_rescan`, racing against `openke_wifi_manual_insert`'s own deliberate power-sequenced SDIO insert (`WIFI_SEQ:` log lines) | n/a | None - the manual, power-sequenced insert immediately afterward succeeds correctly (`mmc1: new high speed SDIO card`) | `TEST_PROFILE_EXPECTED` (already documented as a known, previously-analyzed race - see the earlier WiFi bring-up work) | Not attempted this pass - already classified `KNOWN_BENIGN_PREPOWER_SCAN` by the earlier WiFi work; suppressing it risks destabilizing the proven manual-insert sequence for a purely cosmetic gain | - |
| `ingenic pinctrl 10010000.pinctrl: pin GPC-25 already requested by 10054000.i2c; cannot claim for 10033000.serial` (`uart3` vs `i2c4`) | 1.429389-1.459956 | Already fully documented - `FIRMWARE.md` §54, `docs/DTB_PARITY_REPORT.md` | Stock has a working, separate Bluetooth transport (not `uart3`) alongside working touch (`i2c4`); real hardware is the BCM4343A1 combo chip's BT side, confirmed live via `/dev/ttyS3` and Creality's own `bt_enable_bsa.sh` | None new - Klipper doesn't need `uart3`; touch (`ns2009_ts`) works correctly | `ARCHITECTURE_LIMITATION` (Path A) | **FIXED, pending live verification.** Final cleanup mission Phase 3: `uart3_pc` (`GPC-25/26`) is the same physical pin group as `i2c4_pc` (touch) - confirmed via stock's own live DTB phandle resolution, a real hardware pin-sharing design, not a bug. i2c4/touch is statically "okay" and always wins the race; stock resolves this via a dynamic runtime pin hand-off (re-muxing when `bt_enable_bsa.sh` runs), not a static devicetree difference this project can replicate without a real H5 Bluetooth transport implementation (Path B, a separate, larger effort). Since `uart3` in its previous "okay" state was **guaranteed** to fail this pinctrl claim every single boot (not "might work"), Path A (clean static disable) removes real, unavoidable noise without losing any function - BT never worked over this node regardless of its status. All prior investigation preserved in the DTS comment for whoever implements Path B. | `51b8ecedd` (kernel fork) |
| `jitterentropy: Initialization failed with host not compliant with requirements: 9` | ~0.21 (varies), **absent in the current tagged baseline's own authoritative boot log** | `crypto/jitterentropy-kcapi.c:346` (`pr_info`, not `panic` - `CRYPTO_JITTERENTROPY_OSR=0`/non-fatal path taken); the check itself measures real CPU timing jitter at boot, so its pass/fail outcome is a live hardware-timing measurement, not a fixed function of the kernel binary | n/a | None either way - hardware TRNG (`10072000.dtrng`) registers successfully and is live at `/sys/class/misc/hw_random/`; CRNG inits fast via the real hardware TRNG regardless of this self-test's outcome | `ARCHITECTURE_LIMITATION` (real hardware TRNG makes it moot); final-log status: `JITTERENTROPY_WARNING_NOT_PRESENT_IN_FINAL_BASELINE` | None needed either way - CRNG is healthy and fast-seeding via the real hardware TRNG regardless of whether this message appears on a given boot. Kernel handoff mission (2026-07-23): the tag's own authoritative boot log (`dmesg-functional-production-baseline-final.txt`) has zero matches for this message - confirmed not a capture gap (clean timestamp jump from `0.103896` to `0.236791`, right through where it always previously appeared). No kernel change in this mission touches jitterentropy/RNG code; the most likely explanation is genuine boot-to-boot CPU-timing variance in the self-test itself (a `PREEMPT_RT` MIPS system measuring live jitter), not a fix. Not claimed as fixed; not listed as a remaining warning per the tag's own log. | - |
| `dwc2 13500000.otg: supply vusb_d not found, using dummy regulator` / `vusb_a` | 0.245243, 0.252604 | `drivers/usb/dwc2/core.h:44-45` (mainline, hardcoded optional supply names `vusb_d`/`vusb_a`); confirmed **absent from every board DTS** in this tree (zero matches) | n/a | None - **correction, 2026-07-26**: this cell originally said "USB (storage, HID, camera per driver registration) works throughout this and prior sessions' testing," but driver registration in `dmesg` was never proof of a real external device enumerating - a real, then-unresolved contradiction existed with FIRMWARE.md §42 (no external USB device ever enumerated on this physical unit). Genuinely proven now: a real USB flash drive and a real USB UVC webcam both enumerate correctly, mount/stream, with real hardware attached - see FIRMWARE.md §60 | `OPTIONAL_FEATURE_MISSING` | None planned - these are standard-optional dwc2 regulator names with no board-level supply defined anywhere in this SoC's board files; the rails are almost certainly fixed/always-on, not software-controllable, and USB is now genuinely proven working (FIRMWARE.md §60) | - |
| `ingenic-fb 13050000.dpu: fbdev get swap param error! please check dts.` | 0.862491 | `module_drivers/drivers/video/fbdev/ingenic/fb_stage/sysfs.c:865` (`dev_err`, but display still initializes: `rot_angle=0, swap_dims=0` immediately follow) | n/a - only one other reference board in this tree (`kale_v10.dts`) defines `layer,src-size` at all, confirming this is genuinely board-specific/optional, not a universal requirement | None observed - display, backlight, touch, GuppyScreen all already proven working | `OPTIONAL_FEATURE_MISSING`/driver-logging-severity mismatch (now fixed) | **FIXED, verified live.** `layer,src-size` feeds an optional multi-layer hw-compositor sysfs export (`of_sysfs_parse_export_info()`), not the primary framebuffer path GuppyScreen uses - downgraded `dev_err`→`dev_dbg`, no values changed. Rebuilt/reflashed/rebooted: message gone, `rot_angle=0, swap_dims=0` info line unchanged, display/touch/GuppyScreen unaffected. | `41fec9840` |
| `ingenic-tcu 10002000.tcu: error -ENXIO: IRQ index 1 not found` / `not support irq trigger function` | 0.992635-0.999458 | `module_drivers/drivers/mfd/ingenic-tcu.c:869`; base `x2000.dtsi:158-164` defines only `interrupts = <IRQ_TCU0>` (index 0), no second entry | n/a | None - `ingenic TCU driver register completed` immediately after; watchdog and PWM (both TCU-adjacent) probe successfully elsewhere in the log | `OPTIONAL_FEATURE_MISSING` | **FIXED (severity only), pending live verification.** Final cleanup mission Phase 6: verified by source read that `ret` (set from the failed `platform_get_irq(pdev, 1)`) is unconditionally overwritten a few lines later (`ret = request_irq(tcu->irq, ...)`, the *primary*, always-present IRQ) before it's ever checked - this optional second, trigger-mode IRQ has zero effect on probe success or failure. Downgraded `dev_err`→`dev_info` with a message reflecting that it's optional and genuinely unused by this board; no functional/DT change. | `5ac124ac6` (kernel fork) |
| `This architecture does not have kernel memory protection.` | (init, no timestamp shown) | `init/main.c:1423` - the `#else` (final) branch of a 3-way `#ifdef` ladder gated on `CONFIG_STRICT_KERNEL_RWX`/`CONFIG_ARCH_HAS_STRICT_KERNEL_RWX`; **confirmed `CONFIG_ARCH_HAS_STRICT_KERNEL_RWX` is not defined anywhere in `arch/mips/Kconfig`** in this kernel tree | n/a | None - informational | `ARCHITECTURE_LIMITATION` | None possible - this is a real, mainline MIPS port gap (no generic strict-kernel-RWX infrastructure exists for MIPS in this kernel version), not a configuration choice this project can fix. Userland hardening (`RELRO`, `PIE`, stack protector, `FORTIFY_SOURCE`) is a separate, valid follow-up tracked independently. | - |

## Summary of open work from this inventory

Fixed and verified live on real hardware (rebuild + reflash + reboot, fresh `dmesg` captured after
each):

1. **`ingenic_mac.c` GPIO error-pointer bug + real `GPB-16` pin conflict with `lcd_rst`** - driver fix
   (`0e3b5ab02`) + board-level `&mac1` disable (`9d32ff87e`). `dmesg | grep -c 'invalid GPIO'` → `0`;
   zero `mac1`/`134a0000` matches at all (no probe attempt).
2. **`brcmfmac` board-specific firmware alias** (`24e0e6c`, `5b8018e`) - needed two commits: the
   overlay symlink alone wasn't sufficient, since the request happens before rootfs is mounted (same
   timing class documented below for `regulatory.db`); baking it into `CONFIG_EXTRA_FIRMWARE` closed
   it. The "failed" message for this alias no longer appears in `dmesg` at all.

Partially fixed - packaging correct, boot-time load still fails for a real, understood reason:

3. **`regulatory.db` packaging** (`1cce6e0`) - `BR2_PACKAGE_WIRELESS_REGDB` enabled, files confirmed
   present in the final rootfs with the correct trust-anchor-matching signature, but the boot-time
   message persists: cfg80211 requests it before rootfs is mounted, and unlike the `brcmfmac` alias
   these files aren't reachable from `CONFIG_EXTRA_FIRMWARE` without a dedicated fetch step (they're
   installed by the Buildroot package into `TARGET_DIR`, not this project's own overlay tree). Real,
   scoped follow-up: mirror `fetch-wifi-firmware.sh`'s pattern to land real copies under the overlay.

4. **Framebuffer swap-property DT annotation** (Phase 15) - candidate fix identified, not yet
   implemented. Only remaining open item in this file.

**GPD-4/BAIC4 pin conflict** - root-caused via instrumentation (Phase 3A/3B, boot-cleanup continuation
mission), then resolved as a side effect of the later, separate BAIC4 audio investigation removing
BAIC4's pinctrl claim entirely. Verified live, zero matches for the warning anywhere in a fresh
post-reboot `dmesg`. See `docs/PIN_OWNERSHIP_MAP.md`.

SFC "unknown flash type" - **resolved**: `/proc/mtd`/`/sys/class/mtd` confirmed empty on both stock
and custom; `&sfc` disabled (`212dce2f2`).

Fixed and verified live, final cleanup mission (2026-07-23):

5. **LO0_MUX-LO11_MUX ASoC "incorrect number of controls" (12 warnings)** - dead vendor code
   (`#if 0`/`#endif` around already-correct kcontrol wiring) in `as-dsp.c`, enabled. `4579e3803`.
6. **TCU trigger-IRQ `dev_err`** - downgraded to `dev_info`; verified via source that the return value
   has zero effect on probe success (unconditionally overwritten before being checked). `5ac124ac6`.
7. **`uart3` guaranteed pinctrl-claim failure vs `i2c4`/touch** - Path A: cleanly disabled (was
   guaranteed to fail every boot, not "might work"); full prior Bluetooth/pin-sharing investigation
   preserved in-place for a future Path B (real BT transport). `51b8ecedd`.
8. **MScaler0/MScaler1 "IRQ index 0 not found"** - both disabled; v4l2_subdev-only, zero userspace
   consumer, real camera/rotation/encode/decode paths unaffected. `69d8655d0`.

Fixed and verified live, functional production-baseline mission (2026-07-23) - the final pass, full
smoke test and clean boot-log review passed with zero unexplained messages:

9. **Whole PCM/ALSA audio graph** - stock booted directly and confirmed to never register a sound
   card at all (`ALSA device list: No soundcards found.`); disabled `as-platform`, `as-virtual-fe`,
   `as-fmtcov`, `as-dsp`, `as-baic`, `as-dmic`, `as-mixer`, `as-spdif`, `icodec`, and the top-level
   `sound` machine-driver node. This also supersedes items 5 and (the earlier, since-superseded) DMIC
   fix above - both were real, correct fixes for a subsystem this mission determined should not exist
   on this product at all. PWM beeper entirely unaffected, confirmed live post-reboot. `0ad06fa0e`.
10. **`cpu-ost-map`/`cpu-intc-map` "too large" (2 messages)** - identical off-by-one bounds-check bug
    in two sibling drivers (`ingenic_core_ost.c`, `irq-ingenic-chip.c`); fired on the last legitimate
    entry of this board's fully correct, minimal 2-CPU DT, not a real excess. Fixed by checking the
    target array index before writing instead of after the fact. `2d507671c`, `e123bb14f`.

Closed, no action needed:

- Backup GPT warning (`STOCK_STORAGE_LAYOUT_LIMITATION`, confirmed identical on stock).
- Jitterentropy (`ARCHITECTURE_LIMITATION`, hardware TRNG covers it, CRNG healthy regardless; absent
  from the current tag's own authoritative boot log - not listed as a remaining warning, see the
  table row above for the full reconciliation).
- USB dummy regulators, `pdmam` IRQ (`OPTIONAL_FEATURE_MISSING`, SoC-reference-level omissions with no
  proven consumer impact).
- Architecture memory-protection message (`ARCHITECTURE_LIMITATION`, confirmed via mainline source).
- Initial unpowered Wi-Fi scan (`KNOWN_BENIGN_PREPOWER_SCAN`/`TEST_PROFILE_EXPECTED`, already tracked
  and reaffirmed unchanged - the manual, power-sequenced insert immediately afterward always succeeds;
  suppressing the race risks destabilizing a proven sequence for a purely cosmetic gain).
- CLM/TxCap blobs (`STOCK_EQUIVALENT_ACCEPTED` - confirmed genuinely absent from stock itself via full
  rootfs extraction, not merely "not found here").
- Static GPIO base allocation deprecation warnings, IRQ affinity warning - vendor/mainline-generation
  characteristics with no observable functional effect, both CPUs and all interrupts work correctly.

**Final boot-log gate: PASS.** A fresh, full post-reboot `dmesg` capture on the functional
production-baseline image (`artifacts/parity/baseline-2026-07-23-boot-cleanup/
dmesg-functional-production-baseline-final.txt`) has zero kernel stack traces, zero GPIO conflicts,
zero failed mandatory probes, zero repeated deferred-probe churn, and zero matches for `uart3`,
`mscaler`, `mac1`, `msc2`, `sfc`, `redefinition`, `conflict`, or `already requested`. Every remaining
message is accounted for in this table.
