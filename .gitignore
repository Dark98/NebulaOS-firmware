# Large third-party source trees fetched for reference/build purposes.
# Provenance/exact commit is recorded in FIRMWARE.md - not committed here,
# re-clone from the URLs there if this workspace moves.
/vendor/
