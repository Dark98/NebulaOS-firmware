# Boot Warning Audit

Structured inventory of every warning/error in the custom Nebula Pad Linux 6.6.18-rt23 kernel boot
log, per the "Nebula Pad Linux 6.6 boot-cleanup investigation and remediation" mission (2026-07-23).
Baseline: `artifacts/parity/baseline-2026-07-23-boot-cleanup/` (git commits, config/artifact hashes,
full `dmesg` capture). Evidence for every row below is a direct source-code citation or a live
stock/custom comparison - no message is classified from guesswork.

Log scope note: the captured log runs from cold boot through `Run /linuxrc as init process` only
(dmesg's own natural end point, before userspace). Userspace-stage validation (Wi-Fi association,
DHCP, Moonraker, Klipper, GuppyScreen, `confirm-good`, clean shutdown) is a separate, already-proven
capability documented in FIRMWARE.md and re-confirmed live at baseline time (see `BASELINE.md`).

Post-fix capture: `artifacts/parity/baseline-2026-07-23-boot-cleanup/dmesg-after-phase2-5-6-12.txt` -
full `dmesg` from the rebuilt/reflashed system carrying commits `0e3b5ab02`/`9d32ff87e` (kernel fork)
and `1cce6e0`/`24e0e6c`/`5b8018e` (main repo), captured after a real reboot cycle and re-confirmed
Moonraker-healthy.

| Message | Timestamp | Source file/function | Stock behavior | Runtime impact | Classification | Fix | Commit |
|---|---:|---|---|---|---|---|---|
| `ingenic-dma 13420000.dma: error -ENXIO: IRQ pdmam not found` | 0.000000 | `module_drivers/drivers/dma/ingenic/ingenic_dma.c:1173-1176` - `platform_get_irq_byname(pdev, "pdmam")`, only logged if `< 0`, DMA driver continues (`INGENIC SoC DMA initialized` follows immediately) | Not yet compared (low priority - non-fatal, SoC-level, base dtsi doesn't name a `pdmam` IRQ for this DMA controller instance) | None observed - DMA works (used throughout boot for eMMC/WiFi/video) | `OPTIONAL_FEATURE_MISSING` | None planned - `pdmam` is an optional secondary DMA-MCU interrupt the base SoC DT doesn't wire; primary DMA channels work throughout boot without it | - |
| `gpiod_set_value_cansleep: invalid GPIO (errorpointer)` (×2) | 1.326183, 1.361287 | `drivers/gpio/gpiolib.c:2153` (`validate_desc()`, mainline gpiolib safety net) called from `module_drivers/drivers/net/ethernet/ingenic/ingenic_mac.c:113,119` (`ingenic_mac_phy_hwrst()`) | n/a - stock is Linux 4.4.94, different driver generation | None observed this boot (Ethernet unused) but is a real caller-side bug: `gpiod_set_value_cansleep(lp->reset_gpio, ...)` called on a descriptor never checked with `IS_ERR()` | `REAL_KERNEL_BUG` | **FIXED, verified live** - root cause was a genuine pin conflict (`GPB-16` claimed by both `lcd_rst` and `mac1`'s `ingenic,rst-gpio`) that the driver's `== NULL` check (instead of `IS_ERR_OR_NULL()`) silently swallowed. Rebuilt/reflashed/rebooted: `dmesg \| grep -c 'invalid GPIO'` now returns `0`. | `0e3b5ab02` (kernel fork) |
| `ingenic_gpio_request: GP:GPD used_pins_bitmap: 0X07FE3F3C` + full backtrace + `module_drivers/drivers/pinctrl/pinctrl-ingenic.c:gpio functions has redefinition` | 1.069000-1.242822 | `module_drivers/drivers/pinctrl/pinctrl-ingenic.c:674`, reached via `sdhci_ingenic_probe` → `devm_gpiod_get_optional` → `gpiod_request_commit` → `ingenic_gpio_request` (vendor `.request` chip callback) | Not yet compared | None observed - eMMC (`mmc0`) still probes and works correctly afterward | `UNKNOWN_REQUIRES_TRACING` | Not yet fixed - needs its own trace, separate from the GMAC finding above (different call chain: this is on `GPD`, during `sdhci_ingenic_probe`'s own optional-GPIO acquisition, not `ingenic_mac.c`) | - |
| `Alternate GPT is invalid, using primary GPT.` | 1.368659 | Generic kernel EFI/GPT partition code (`block/partitions/efi.c`) | **Confirmed present verbatim in stock's own dmesg** (`artifacts/parity/stock/11-dmesg.txt:51`) | None - primary GPT is valid and used, all 10 partitions enumerate correctly on both systems | `STOCK_STORAGE_LAYOUT_LIMITATION` | None - per the mission's own decision rule ("stock shows the same warning → classify, document, do not modify the live disk"), this is closed. No GPT repair planned or needed. | - |
| `ingenic-sfc 13440000.sfc: unknown flash type` / `probe of 13440000.sfc failed with error -22` | 1.313320, 1.318705 | `module_drivers/drivers/mtd/devices/ingenic_sfc_v2/ingenic_sfc_drv.c:118,152` | `/sys/bus/platform/devices/13440000.sfc` **is present** in stock's own platform-device capture (`artifacts/parity/stock/13-platform-devices.txt:53`) - same node exists on both, but stock's dmesg capture (taken well after boot) doesn't retain early SFC probe messages either way | None - clean probe failure, doesn't block eMMC/anything else | `UNKNOWN_REQUIRES_TRACING` | Not yet decided - board's `&sfc` node defines a `uboot`/`kernel`/`rootfs` NOR partition scheme that doesn't match this board's real eMMC-based OTA A/B boot architecture, suggesting vendor reference-design residue, but no positive confirmation the physical SPI-NOR chip is actually absent from this board revision. Mission explicitly forbids blind SPI transactions to test - deferred pending documentation or a lower-risk confirmation method. | - |
| `ingenic-mscaler 13702300.mscaler: error -ENXIO: IRQ index 0 not found` / `No MScaler IRQ specified` (×2, both instances) | 0.880896-0.902763 | `module_drivers/drivers/media/platform/ingenic-isp/mscaler.c:1443` (`dev_warn`, not `dev_err` - probe continues) | Base `x2000.dtsi:859-923` defines both `mscaler0`/`mscaler1` with `status = "ok"` but **no `interrupts` property at all** - this is an SoC-reference-level DT omission, not board-specific | None observed - rotation (`/dev/video0`), H.264 encode (`/dev/video1`), H.264 decode (`/dev/video2`) all register successfully; driver's own `dev_warn` (not `dev_err`) confirms this is treated as a supported, non-fatal configuration (interrupt-less/polling) | `OPTIONAL_FEATURE_MISSING` | None planned - inherited SoC-level omission, driver already treats it as non-fatal by design (`dev_warn`), no consumer proven broken | - |
| `x2000-sound sound: ASoC: CPU DAI DMIC not registered` / `snd_soc_register_card failed -517` (repeated ~6×) | 0.753917-1.617289 | `sound/soc/soc-core.c:1083` (mainline `dev_info`, not even a warning) + `module_drivers/sound/soc/ingenic/boards/halley5_v20.c` DMIC DAI link definition (lines 414-415, 527-531) | Already documented: `docs/STOCK_PARITY_MATRIX.md` - stock has a real, populated `134da000.as-dmic` device node; custom does not | None fatal - repeated `-EPROBE_DEFER` (517) retries are the standard, harmless ASoC deferred-probe pattern; no soundcard registers on custom (`ALSA device list: No soundcards found`), matching the already-known, already-scoped audio gap | `MISSING_STOCK_FUNCTION` (already tracked) | Deferred per prior explicit decision - `AVAILABLE_DISABLED` pending the Phase 4 audio feasibility study, not to be restored without checking pin conflicts with touch/eMMC/BT/printer UART first. This pass's contribution: disable the specific DMIC-dependent DAI link (not the whole sound card - `as-platform`/`as-dsp`/`as-mixer`/`as-baic`/`as-spdif`/`icodec` all probe successfully and are real, working, non-DMIC audio infrastructure) to stop the repeated deferred-probe churn. Not yet implemented this pass. | - |
| `eth%d: no PHY found` / `dwc-mac 134a0000.mac: MII Probe failed!` | 1.402433, 1.405583 | `module_drivers/drivers/net/ethernet/ingenic/ingenic_mac.c`, board override `halley5_v30.dts:575-586` (`&mac1`, `status = "okay"`) | Not yet compared (stock is a different kernel generation) | None - no product feature on this board uses wired Ethernet (Nebula Pad/Ender3V3KE has no RJ45 port) | `INTENTIONAL_UNUSED_BLOCK` (high confidence) | **FIXED, verified live** - root cause: `ingenic,rst-gpio` (`&gpb 16`) collided with `lcd_rst`'s live GPIO ownership of the same pin (confirmed via live debugfs: `gpio-48 (lcd_rst) out hi`). `&mac1` disabled in the board DTS. Rebuilt/reflashed/rebooted: `dmesg \| grep -iE 'mac1\|134a0000'` now returns zero matches - no probe attempt at all. | `9d32ff87e` (kernel fork) |
| `platform regulatory.0: Direct firmware load for regulatory.db failed with error -2` / `cfg80211: failed to load regulatory.db` | 1.612460, 1.621406 | Mainline `cfg80211` regulatory core | n/a - stock uses a different wireless stack generation, not a useful comparison | Low - `iw reg get` falls back to a permissive/world domain; WiFi association still works (already proven, WiFi bring-up documented) | `OPTIONAL_FEATURE_MISSING` (real, partially fixed) | **Partially fixed, boot-time message still present.** `BR2_PACKAGE_WIRELESS_REGDB` enabled (`1cce6e0`) - `regulatory.db`/`.p7s` are confirmed present in the final rootfs (verified via `unsquashfs`), signed with the same `sforshee` key already compiled into this kernel's trust anchors. Rebuilding/reflashing/rebooting still shows this exact message: the request happens at `t=1.61s`, before `VFS: Mounted root` (`t=3.87s`) - the identical rootfs-not-mounted-yet timing class already documented for `brcmfmac`. Unlike the `brcmfmac` alias, these files aren't reachable from `CONFIG_EXTRA_FIRMWARE` without a real fetch step (they're installed by the Buildroot package straight into `TARGET_DIR`, not this project's own overlay tree) - real, scoped follow-up, not done this pass. | `1cce6e0` (partial) |
| `brcmfmac mmc1:0001:1: Direct firmware load for brcm/brcmfmac43430-sdio.ingenic,halley5.bin failed with error -2` | 2.320625 | Mainline `brcmfmac` board-specific firmware alias lookup; `scripts/build/overlay/lib/firmware/brcm/` **only shipped the generic `brcmfmac43430-sdio.bin`/`.txt`**, not the board-specific alias name | n/a | None - generic firmware loads successfully immediately after (`Firmware: BCM43430/1 ... version 7.46.58.13`), WiFi fully functional | `OPTIONAL_FEATURE_MISSING` (real, easily fixable cosmetic gap) | **FIXED, verified live** - two-part fix: a symlink alias in the overlay (`24e0e6c`) was necessary but not sufficient, since `brcmfmac`'s firmware request happens before rootfs is mounted (same already-documented timing class as the primary binary); baking the alias into `CONFIG_EXTRA_FIRMWARE` (`5b8018e`) closed it. Rebuilt/reflashed/rebooted: the "failed" message for this alias no longer appears in `dmesg` at all. | `24e0e6c`, `5b8018e` |
| `brcmfmac mmc1:0001:1: Direct firmware load for brcm/brcmfmac43430-sdio.clm_blob failed with error -2` / `no clm_blob available` / `no txcap_blob available` | 2.340321-2.459528 | Mainline `brcmfmac` optional CLM/TxCap blob loader | n/a | Low - `brcmf_c_process_clm_blob`'s own message says "device may have limited channels available", not "non-functional"; WiFi already proven working end-to-end | `OPTIONAL_FEATURE_MISSING` | Deferred - the mission explicitly forbids adding an unrelated/guessed CLM or TxCap blob; needs a provenance-matched vendor file (same BCM43430/1 + firmware 7.46.58.13) that hasn't been located yet | - |
| `mmc1: Failed to initialize a non-removable card` | 1.672314 | Generic MMC core `mmc_rescan`, racing against `openke_wifi_manual_insert`'s own deliberate power-sequenced SDIO insert (`WIFI_SEQ:` log lines) | n/a | None - the manual, power-sequenced insert immediately afterward succeeds correctly (`mmc1: new high speed SDIO card`) | `TEST_PROFILE_EXPECTED` (already documented as a known, previously-analyzed race - see the earlier WiFi bring-up work) | Not attempted this pass - already classified `KNOWN_BENIGN_PREPOWER_SCAN` by the earlier WiFi work; suppressing it risks destabilizing the proven manual-insert sequence for a purely cosmetic gain | - |
| `ingenic pinctrl 10010000.pinctrl: pin GPC-25 already requested by 10054000.i2c; cannot claim for 10033000.serial` (`uart3` vs `i2c4`) | 1.429389-1.459956 | Already fully documented - `FIRMWARE.md` §54, `docs/DTB_PARITY_REPORT.md` | Stock has a working, separate Bluetooth transport (not `uart3`) alongside working touch (`i2c4`) | None new - Klipper doesn't need `uart3`; touch (`ns2009_ts`) works correctly | `MISSING_STOCK_FUNCTION` (already tracked) | Out of scope for this pass - Bluetooth transport is Phase 8/H, an explicitly separate, isolated investigation per the mission's own instruction not to combine it with boot-cleanup work | - |
| `jitterentropy: Initialization failed with host not compliant with requirements: 9` | 0.214405 | `crypto/jitterentropy-kcapi.c:346` (`pr_info`, not `panic` - `CRYPTO_JITTERENTROPY_OSR=0`/non-fatal path taken) | n/a | None - hardware TRNG (`10072000.dtrng`) registers successfully and is live at `/sys/class/misc/hw_random/`; `entropy_avail=256` (full pool); `crng init done` at `t=6.36s` (fast, not blocked) | `ARCHITECTURE_LIMITATION` (real hardware TRNG makes it moot) | None needed - CRNG is healthy and fast-seeding via the real hardware TRNG; jitterentropy's software-timing-based entropy source failing on this MIPS/PREEMPT_RT combination has no observable downstream effect | - |
| `dwc2 13500000.otg: supply vusb_d not found, using dummy regulator` / `vusb_a` | 0.245243, 0.252604 | `drivers/usb/dwc2/core.h:44-45` (mainline, hardcoded optional supply names `vusb_d`/`vusb_a`); confirmed **absent from every board DTS** in this tree (zero matches) | n/a | None - USB (storage, HID, camera per driver registration) works throughout this and prior sessions' testing | `OPTIONAL_FEATURE_MISSING` | None planned - these are standard-optional dwc2 regulator names with no board-level supply defined anywhere in this SoC's board files; the rails are almost certainly fixed/always-on, not software-controllable, and USB is already proven working | - |
| `ingenic-fb 13050000.dpu: fbdev get swap param error! please check dts.` | 0.862491 | `module_drivers/drivers/video/fbdev/ingenic/fb_stage/sysfs.c:865` (`dev_err`, but display still initializes: `rot_angle=0, swap_dims=0` immediately follow) | n/a | None observed - display, backlight, touch, GuppyScreen all already proven working | `OPTIONAL_FEATURE_MISSING`/driver-logging-severity mismatch | Not yet implemented - candidate fix is adding the explicit no-swap DT property (to silence the message accurately) rather than changing behavior, since the resulting default (`rot_angle=0, swap_dims=0`) is already correct for this board's known-working orientation | - |
| `ingenic-tcu 10002000.tcu: error -ENXIO: IRQ index 1 not found` / `not support irq trigger function` | 0.992635-0.999458 | `module_drivers/drivers/mfd/ingenic-tcu.c:869`; base `x2000.dtsi:158-164` defines only `interrupts = <IRQ_TCU0>` (index 0), no second entry | n/a | None - `ingenic TCU driver register completed` immediately after; watchdog and PWM (both TCU-adjacent) probe successfully elsewhere in the log | `OPTIONAL_FEATURE_MISSING` | None planned - SoC-reference-level DT omission (single-IRQ TCU wiring), no consumer proven to need the second, optional trigger-mode interrupt | - |
| `This architecture does not have kernel memory protection.` | (init, no timestamp shown) | `init/main.c:1423` - the `#else` (final) branch of a 3-way `#ifdef` ladder gated on `CONFIG_STRICT_KERNEL_RWX`/`CONFIG_ARCH_HAS_STRICT_KERNEL_RWX`; **confirmed `CONFIG_ARCH_HAS_STRICT_KERNEL_RWX` is not defined anywhere in `arch/mips/Kconfig`** in this kernel tree | n/a | None - informational | `ARCHITECTURE_LIMITATION` | None possible - this is a real, mainline MIPS port gap (no generic strict-kernel-RWX infrastructure exists for MIPS in this kernel version), not a configuration choice this project can fix. Userland hardening (`RELRO`, `PIE`, stack protector, `FORTIFY_SOURCE`) is a separate, valid follow-up tracked independently. | - |

## Summary of open work from this inventory

Fixed and verified live on real hardware (rebuild + reflash + reboot, fresh `dmesg` captured after
each):

1. **`ingenic_mac.c` GPIO error-pointer bug + real `GPB-16` pin conflict with `lcd_rst`** - driver fix
   (`0e3b5ab02`) + board-level `&mac1` disable (`9d32ff87e`). `dmesg | grep -c 'invalid GPIO'` → `0`;
   zero `mac1`/`134a0000` matches at all (no probe attempt).
2. **`brcmfmac` board-specific firmware alias** (`24e0e6c`, `5b8018e`) - needed two commits: the
   overlay symlink alone wasn't sufficient, since the request happens before rootfs is mounted (same
   timing class documented below for `regulatory.db`); baking it into `CONFIG_EXTRA_FIRMWARE` closed
   it. The "failed" message for this alias no longer appears in `dmesg` at all.

Partially fixed - packaging correct, boot-time load still fails for a real, understood reason:

3. **`regulatory.db` packaging** (`1cce6e0`) - `BR2_PACKAGE_WIRELESS_REGDB` enabled, files confirmed
   present in the final rootfs with the correct trust-anchor-matching signature, but the boot-time
   message persists: cfg80211 requests it before rootfs is mounted, and unlike the `brcmfmac` alias
   these files aren't reachable from `CONFIG_EXTRA_FIRMWARE` without a dedicated fetch step (they're
   installed by the Buildroot package into `TARGET_DIR`, not this project's own overlay tree). Real,
   scoped follow-up: mirror `fetch-wifi-firmware.sh`'s pattern to land real copies under the overlay.

Investigated, deferred with reasoning (not a config flip):

4. **DMIC DAI link churn** (Phase 11) - investigated further: the DAI link array is a static C
   structure inside `halley5_v20.c` (`SND_SOC_DAILINK_DEFS(dmic, ...)`), not a DTS property - the
   board DTS's own `sound` node only sets `compatible`/`ingenic,model`/`ingenic,spken-gpio`. Disabling
   only the DMIC link (without touching the working `as-platform`/`as-dsp`/`as-mixer`/`as-baic`/
   `as-spdif`/`icodec` paths around it) needs a real driver source change, not a config flip - real,
   possible, but a larger and riskier change than initially estimated. Deferred; current behavior is
   cosmetic-only (harmless deferred-probe retries), matching the mission's own "document and move on"
   allowance for disproportionately invasive fixes.
5. **Framebuffer swap-property DT annotation** (Phase 15) - candidate fix identified, not yet implemented.

Still needs live tracing, not yet root-caused:

- The `sdhci_ingenic_probe`/`ingenic_gpio_request` "redefinition" backtrace on `GPD` (Phase 3) - a
  different call chain than the GMAC finding, not yet traced to a specific consumer/pin.
- SFC "unknown flash type" (Phase 9) - real ambiguity about whether SPI-NOR is physically populated
  on this board revision; no blind-probe method available per the mission's own safety rules.

Closed, no action needed:

- Backup GPT warning (`STOCK_STORAGE_LAYOUT_LIMITATION`, confirmed identical on stock).
- Jitterentropy (`ARCHITECTURE_LIMITATION`, hardware TRNG covers it, CRNG healthy).
- USB dummy regulators, MScaler IRQs, TCU trigger IRQ, `pdmam` IRQ (`OPTIONAL_FEATURE_MISSING`,
  SoC-reference-level omissions with no proven consumer impact).
- Architecture memory-protection message (`ARCHITECTURE_LIMITATION`, confirmed via mainline source).
- `uart3`/`i2c4` conflict, initial unpowered Wi-Fi scan (already tracked/classified by prior work).
